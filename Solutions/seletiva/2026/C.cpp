#include <bits/stdc++.h>

using namespace std;

using ll = long long;
using vi = vector<int>;
using vll = vector<long long>;

void solve(){
    int n, m; cin >> n >> m;
    vector<int> d(n);
    for (int& a : d) cin >> a;
    
}

int main(){
    
    int tc; cin >> tc; while(tc--) solve();
    //solve();
}